# NPM image channel configuration
# Used by: npm-helper set-channel stable|edge
# Default channel is stable (pinned, tested tag).

[channel]
# stable = pinned, tested tag; edge = latest upstream
channel = stable
stable_tag = 2.13.5
edge_tag = latest
